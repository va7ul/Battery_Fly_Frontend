export const OrderDetailsList = () => {
  //   const data = {};

  return (
    <>
      {/* {data.map(el => (
        <li key={el.number}>
          <OrdersHistoryListEl el={el} />
        </li>
      ))} */}
    </>
  );
};
