import { ReactNode } from "react";

export type PropsChildren = {
  children: ReactNode;
};

export type Service = {
  title: string;
  icon: string;
  text: string;
};


export type Reason = {
  icon: string;
  text: string;
};